# Sample Python API app using Bottle

### Usage
* install Python modules from requirements.txt using pip
* run ./start_server.sh to run the API app
* run ./requestsh in another terminal window to trigger a request
